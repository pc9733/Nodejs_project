# Prefer the env-specific files:
#   kubectl apply -f k8s/addons/external-secrets-dev.yaml
#   kubectl apply -f k8s/addons/external-secrets-prod.yaml
#
# This combined file is kept only as a reference. Do not apply it on a single
# cluster — it targets namespaces that may not exist on that cluster.
